const SETTINGS = {
  useFixtures: false,
  ezLogin: false,
  yellowBox: "",
  reduxLogging: process.env.NODE_ENV === 'development' ? true : false,
  includeExamples: '',
  useReactotron: true
}

export default SETTINGS
